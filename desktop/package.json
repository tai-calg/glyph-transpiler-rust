{
  "name": "glyph-desktop",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "python3 scripts/build_sidecar.py --development && tauri dev",
    "build": "python3 scripts/build_sidecar.py && tauri build",
    "check": "python3 scripts/build_sidecar.py --placeholder && cargo check --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.4"
  }
}
