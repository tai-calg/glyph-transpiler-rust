# Changelog

## Unreleased

- Add versioned `glyph.ui-ir` and generic Gradio rendering for pure Glyph functions.
- Add safe JSON-only `glyph.ui-manifest` presentation overrides keyed by semantic node IDs.
- Add the public `GlyphUiProject`, renderer registry, and lazy optional frontend boundary.
- Add the distributable `glyph-gradio` console command and `glyph-rust[ui]` package extra.
- Add wheel, source-distribution, installed-command, and multi-application renderer validation.

## 0.4.0

- Add opt-in capability and stateful resource analysis.
- Add apostrophe-prefixed kinded Contract namespace.
- Add World, Protocol, Handler, Law, and Bundle semantics.
- Add symbolic resource-flow and verification-class IR.
- Preserve the existing Plain Glyph compilation path and artifact shape.
- Keep PR #10 draft and unmerged pending explicit review.
